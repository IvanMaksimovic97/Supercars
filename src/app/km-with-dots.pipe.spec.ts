import { KmWithDotsPipe } from './km-with-dots.pipe';

describe('KmWithDotsPipe', () => {
  it('create an instance', () => {
    const pipe = new KmWithDotsPipe();
    expect(pipe).toBeTruthy();
  });
});
